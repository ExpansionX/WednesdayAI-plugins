Report file not written: this session is read-only and the request also said not to modify files. Markdown content:

| severity | task | file:line | issue | remediation |
|---|---|---|---|---|
| Critical | 001 | `README.md:18-22`; `docs/plans/plugin-skillweaver/phase-1/001-plugin-scaffold.md:10-12`, `:34`, `:68` | The scaffold uses `extensions/skillweaver` and package name `@wednesdayai/skillweaver`, but this repo’s documented plugin convention is `plugins/<plugin-id>/` and `@wednesdayai-plugins/<plugin-id>`. Implementers will build the new plugin outside the repo’s plugin layout. | Rewrite all task paths, gate commands, imports, and package name to `plugins/skillweaver` / `@wednesdayai-plugins/skillweaver`, or first update the repo convention in README and make that an explicit irreversible decision. |
| High | 001/009/015/016 | `docs/plans/plugin-skillweaver/phase-1/001-plugin-scaffold.md:9-15`, `:20`, `:282-285`, `:295`; `phase-3/009-index-rebuild.md:9-14`, `:18`, `:182-184`; `phase-6/015-error-resilience.md:9-15`, `:91`, `:197-199`; `phase-6/016-lifecycle.md:9-14`, `:42`, `:139-141` | Multiple tasks require creating or editing test files that are not in `files:` while `allowed_change`/Allowed moves say only listed source files may change. A strict implementer either violates the boundary or cannot make the required gate pass. | Add every required test file to each task’s `files:` list, or declare a consistent rule that `scope_test` files are allowed edits despite `files:`. |
| High | 005 | `docs/superpowers/specs/2026-07-06-plugin-skillweaver.md:59`, `:71`; `docs/plans/plugin-skillweaver/phase-1/001-plugin-scaffold.md:78-80`; `docs/plans/plugin-skillweaver/phase-2/005-local-embedding.md:129`, `:157-160` | SC6 requires local embeddings via `@xenova/transformers`, but the scaffold package only installs `hnswlib-node`; task 005 imports `@xenova/transformers` while its allowed files exclude `package.json`. Typecheck/runtime can fail with no permitted dependency edit. | Put `@xenova/transformers` in task 001 dependencies, or add `extensions/skillweaver/package.json` to task 005 with `allowed_change: edit`. |
| High | 010/012 | `docs/plans/plugin-skillweaver/phase-4/010-decomposer.md:6`, `:198`; `docs/plans/plugin-skillweaver/phase-5/012-context-injector.md:6`, `:111`; `docs/plans/plugin-skillweaver/phase-2/004-embedding-types.md:60-103` | Tasks 010 and 012 import types created by task 004 but do not depend on 004. Scheduled literally, either task can run after 001/002 and before the type file exists. | Add `004` to `depends_on` for 010 and 012. |
| Critical | 008/014 | `docs/superpowers/specs/2026-07-06-plugin-skillweaver.md:95-98`, `:159-166`; `docs/plans/plugin-skillweaver/phase-3/008-skill-index.md:4`; `docs/plans/plugin-skillweaver/phase-5/014-register-wiring.md:185-187`, `:243-258` | The spec requires building an index from actual skill metadata at startup, and task 008 even says “discover skills from config”; task 014 leaves `discoverSkills()` as `return []` and explicitly says discovery is out of scope. SC1/SC2 are hollow because the live pipeline indexes zero skills. | Add a dedicated discovery task before 014, or implement real discovery in 014 using the WednesdayAI skill catalog/config paths and test that `index.build()` receives non-empty parsed skills. |
| Critical | 010 | `docs/superpowers/specs/2026-07-06-plugin-skillweaver.md:278-285`; `dev-docs/adr/0003-sad-prompt-contract.md:28`, `:33`, `:55`, `:58`; `docs/plans/plugin-skillweaver/phase-4/010-decomposer.md:224-240`, `:247-269` | The irreversible SAD contract is JSON object `{ subTasks: string[] }`, but task 010 prompts for and parses a bare JSON array. This breaks the frozen decomposer protocol. | Change tests, prompts, and parser to emit/extract `{ "subTasks": [...] }`; keep array-only parsing only as backward-compatible fallback if explicitly allowed. |
| High | 004 | `docs/superpowers/specs/2026-07-06-plugin-skillweaver.md:270-276`; `dev-docs/adr/0002-pluggable-embedding-backend-interface.md:22-31`; `docs/plans/plugin-skillweaver/phase-2/004-embedding-types.md:11`, `:65-71` | Task 004 creates the `EmbeddingBackend` interface but marks `irreversible: false`, contradicting D2/ADR-0002’s irreversible interface-contract decision. | Mark task 004 `irreversible: true` and cite ADR-0002 in the task header or body. |
| High | 009 | `docs/plans/plugin-skillweaver/phase-3/009-index-rebuild.md:141-160` | The proposed `watch()` calls `import("node:fs").watch(...)` without awaiting/importing `watch`, never assigns `this.watcher`, then returns `this.watcher`; the method returns `null` and/or typechecks incorrectly instead of starting a watcher. | Use `import { existsSync, watch } from "node:fs"` at top level, return `null` when the dir is missing, assign `this.watcher = watch(...)`, and close any previous watcher before replacing it. |
| High | 015 | `docs/plans/plugin-skillweaver/phase-6/015-error-resilience.md:24-42`, `:106-119`, `:179-195` | The timeout tests are hollow: both race the unresolved implementation against an external already/soon-resolving fallback, so they pass even if `handler()`/`decompose()` never implements timeout. The change only threads an optional `signal`; no handler creates an `AbortController` or deadline. | Add a real timeout option, create/abort an `AbortController` in the handler/decomposer path, await the actual handler promise under fake timers, and assert it resolves `{}` plus passes an aborted signal to fetch. |
| Medium | 016/018 | `docs/plans/plugin-skillweaver/phase-6/016-lifecycle.md:130-136`; `docs/plans/plugin-skillweaver/phase-7/018-integration-tests.md:87-93`, `:174-179` | Task 016 adds a second `api.on("agent_end", ...)` hook, but task 018 still asserts `api.on` was called exactly once. Its own STOP trigger later allows `agent_end`, so the integration task conflicts with its dependency. | Update task 018 to assert only allowed hook names are registered: `context.collect` and `agent_end`; do not use `toHaveBeenCalledTimes(1)`. |
| High | 017 | `docs/superpowers/specs/2026-07-06-plugin-skillweaver.md:44-47`, `:61-62`; `docs/plans/plugin-skillweaver/phase-7/017-benchmark.md:432-491`, `:503-509` | The benchmark file only exports `runBenchmark`; it does not load the 50-query fixture, instantiate real or mock components, print rates, or fail when CatR@10/DA thresholds miss. The Done command can exit 0 without proving SC1, SC2, or SC7. | Add a CLI entrypoint that loads `benchmark-queries.json`, runs the real configured pipeline or deterministic benchmark mocks, prints summary, and exits non-zero unless `catR10Rate >= 0.65`, `daExactRate >= 0.60`, and latency thresholds pass. |
| Medium | 012 | `docs/superpowers/specs/2026-07-06-plugin-skillweaver.md:174-180`; `docs/plans/plugin-skillweaver/phase-5/012-context-injector.md:49-52`, `:160-163` | The spec’s contribution source is `"plugin-skillweaver"`, but task 012 tests and implements `source: "skillweaver"`. Downstream diagnostics/filtering by source will not match the frozen contract. | Align task 012 and dependent tests to `source: "plugin-skillweaver"` or amend the spec before implementation. |
| Medium | 018 | `docs/superpowers/specs/2026-07-06-plugin-skillweaver.md:53-56`; `docs/plans/plugin-skillweaver/phase-7/018-integration-tests.md:70-84`, `:87-93` | The SC4 cache test sends `"hi"`, which is below `minQueryLength`, then only checks `systemPrompt` if the result is non-empty; it passes if no injection ever occurs. The SC5 slash-command test only checks no `before_tool_call` hook, not that `/github` or `/weather` still resolve. | Use a long query with mocked retrieval that forces `prependContext`, assert no system prompt mutation on that non-empty contribution, and add a real or SDK-level slash-command resolution fixture for representative slash commands. |

FINDINGS: 12

Self-assessment: Each finding ties a concrete implementer failure to cited spec/task/repo lines and gives an applicable edit to the breakdown, not speculative implementation advice.

